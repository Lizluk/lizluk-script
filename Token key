--// LIZLUK GHOST GUI 🦋 SOLARA EDITION + TOKEN AUTH + ANTI-LOG
-- Optimized for Solara executor with authentication
repeat task.wait() until game:IsLoaded()
task.wait(3)
print("🚀 LIZLUK Loading...")

---------------- 🛡️ ANTI-LOG PROTECTION ----------------
-- Chặn Remote Spy
pcall(function()
    for i,v in pairs(getgc(true)) do
        if type(v) == "table" and rawget(v, "FireServer") then
            v.FireServer = function() end
            v.InvokeServer = function() end
        end
    end
end)

-- Hook namecall để chặn log
local old_namecall
old_namecall = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    
    -- Chặn các method ghi log
    if method == "FireServer" or method == "InvokeServer" then
        -- Kiểm tra nếu là Remote của hệ thống log thì chặn
        local remoteName = tostring(self)
        if remoteName:lower():match("log") or remoteName:lower():match("analytics") then
            return
        end
    end
    
    return old_namecall(self, ...)
end))

-- Chặn HttpSpy
if hookfunction then
    local old_httpget = game.HttpGet
    hookfunction(game.HttpGet, newcclosure(function(self, url, ...)
        -- Cho phép request của script bạn, chặn các spy tool
        if url:match("your-server.com") then -- Thay bằng domain của bạn nếu có
            return old_httpget(self, url, ...)
        end
        return ""
    end))
end

-- Anti-decompile
if getscriptclosure then
    local old_getscript = getscriptclosure
    getscriptclosure = newcclosure(function(...)
        return function() end
    end)
end

print("🛡️ Anti-Log Active")

---------------- SERVICES ----------------
local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

---------------- 🔐 TOKEN + USER AUTHENTICATION ----------------
local REQUIRED_TOKEN = "lizluk-ghost-2026"
local ALLOWED_USERID = 8076792445 -- <<< ĐỔI USERID CỦA BẠN

-- Check token
if not _G.LIZLUK_TOKEN or _G.LIZLUK_TOKEN ~= REQUIRED_TOKEN then
    player:Kick("⛔ Invalid Token! Use: _G.LIZLUK_TOKEN = 'lizluk-ghost-2026'")
    return
end

-- Check user
if player.UserId ~= ALLOWED_USERID then 
    player:Kick("⛔ User not authorized | UserID: " .. player.UserId)
    return 
end

print("✅ Token valid")
print("✅ User authorized:", player.Name)

---------------- SOLARA-SAFE CLAMP ----------------
local function safeClamp(val, min, max)
    if val < min then return min end
    if val > max then return max end
    return val
end

---------------- CODE CHÍNH CỦA BẠN TIẾP TỤC Ở ĐÂY ----------------
-- Phần còn lại của script...
